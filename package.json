{
  "scripts": {
    "build": "npm install --prefix themes/namrider && hugo --minify && npx pagefind --source public"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.13",
    "postcss": "^8.4.19",
    "postcss-cli": "^10.1.0"
  },
  "dependencies": {
    "critical": "^7.2.1",
    "glob": "^10.4.5"
  }
}
